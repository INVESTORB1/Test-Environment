<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Loan Application</title>
    <link rel="stylesheet" href="/public/styles.css">
  </head>
  <body class="lab-detail-page">
    <header>
      <h1>Loan Application</h1>
    </header>
    <div class="card">
      <% if (status) { %>
        <div id="pageBanner" class="banner <%= status === 'success' ? 'banner-success' : 'banner-error' %>" role="status"><%= msg %></div>
      <% } %>
      <p>A quick loan sandbox. Fill the form below to request a small loan for your sandbox accounts.</p>
      <form method="post" action="/labs/2/apply">
        <label>Your name: <input name="name" required maxlength="80" value="<%= userDisplay || '' %>"></label>
        <label>Amount (Naira): <input name="amount" required inputmode="numeric" value="1000"></label>
        <label>Monthly income (Naira): <input name="income" required inputmode="numeric" value="50000"></label>
        <label>Term (months): <input name="term" required inputmode="numeric" value="3"></label>
        <button type="submit" class="btn btn-primary">Apply</button>
      </form>
      <p style="margin-top:12px"><a href="/labs">Back to Labs</a></p>
    </div>
  </body>
</html>
